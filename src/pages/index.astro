---
import Trim from '@components/Trim.astro';
import Layout from '@layouts/Layout.astro';
import type { LayoutProps } from '@sharedTypes/index';

const layoutProps: LayoutProps = {
  meta: {},
  bodyClass: 'index',
};
---

<Layout {...layoutProps}>
  <Trim />
</Layout>

<style lang="scss">
</style>